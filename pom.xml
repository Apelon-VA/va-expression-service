<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>gov.vha.solor</groupId>
        <artifactId>solor-goods</artifactId>
        <version>1.0-SNAPSHOT</version>
    </parent>
    <artifactId>expression-service</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>jar</packaging>
    <properties>
        <ochre.version>2.15</ochre.version>
        <snorocket-core.version>2.5.7</snorocket-core.version>
        <newtons-cradle.version>1.6-SNAPSHOT</newtons-cradle.version>
        <isaac-metadata.version>1.17</isaac-metadata.version>
        <query.version>2.0-SNAPSHOT</query.version>
        <logic.version>1.2-SNAPSHOT</logic.version>
    </properties>
    <dependencies>
        <dependency>
            <groupId>gov.vha.solor</groupId>
            <artifactId>snomed</artifactId>
            <version>1.0-SNAPSHOT</version>
            <type>lucene.zip</type>
            <classifier>all</classifier>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>gov.vha.solor</groupId>
            <artifactId>snomed</artifactId>
            <version>1.0-SNAPSHOT</version>
            <type>cradle.zip</type>
            <classifier>all</classifier>
            <scope>compile</scope>
        </dependency>
        <dependency>
            <groupId>gov.vha.isaac</groupId>
            <artifactId>newtons-cradle-extension</artifactId>
            <version>${newtons-cradle.version}</version>
        </dependency>
        <dependency>
            <groupId>gov.vha.isaac</groupId>
            <artifactId>query-implementation</artifactId>
            <version>${query.version}</version>
        </dependency>
        <dependency>
            <groupId>gov.vha.isaac</groupId>
            <artifactId>logic</artifactId>
            <version>${logic.version}</version>
        </dependency>
    </dependencies>
    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <execution>
                        <id>extract-data</id>
                        <phase>generate-test-resources</phase>
                        <goals>
                            <goal>unpack-dependencies</goal>
                        </goals>
                        <configuration>
                            <includeArtifactIds>snomed</includeArtifactIds>
                            <outputDirectory>${project.build.directory}/data</outputDirectory>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <version>1.3.2</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>java</goal>
                        </goals>
                    </execution>
                </executions>
                <configuration>
                    <mainClass>gov.vha.isaac.expression.service.Main</mainClass>
                    <arguments>
                        <argument>${project.build.directory}</argument>
                    </arguments>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>